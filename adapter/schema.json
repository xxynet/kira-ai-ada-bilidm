{
  "bot_uid": {
    "name": "Bot UID",
    "type": "string",
    "default": "",
    "hint": "Bot's BiliBili UID",
    "locales": {
      "zh": { "name": "机器人UID", "hint": "机器人账号的B站UID" }
    }
  },
  "permission_mode": {
    "name": "Permission Mode",
    "type": "string",
    "options": ["allow_list", "deny_list"],
    "default": "allow_list",
    "hint": "Allow list or deny list",
    "locales": {
      "zh": { "name": "权限模式", "hint": "白名单（allow_list）或黑名单（deny_list）模式" }
    }
  },
  "user_allow_list": {
    "name": "User Allow List",
    "type": "list",
    "default": [],
    "hint": "Takes effect when permission mode is allow_list",
    "locales": {
      "zh": { "name": "用户白名单", "hint": "用户UID列表，当权限模式为白名单时生效" }
    }
  },
  "user_deny_list": {
    "name": "User Deny List",
    "type": "list",
    "default": [],
    "hint": "Takes effect when permission mode is deny_list",
    "locales": {
      "zh": { "name": "用户黑名单", "hint": "用户UID列表，当权限模式为黑名单时生效" }
    }
  },
  "sesdata": {
    "name": "sesdata",
    "type": "string",
    "default": "",
    "hint": "sesdata (from Cookie)",
    "locales": {
      "zh": { "name": "SESSDATA", "hint": "B站Cookie中的SESSDATA字段" }
    }
  },
  "bili_jct": {
    "name": "bili_jct",
    "type": "string",
    "default": "",
    "hint": "bili_jct (from Cookie)",
    "locales": {
      "zh": { "name": "bili_jct", "hint": "B站Cookie中的bili_jct字段（CSRF Token）" }
    }
  },
  "buvid3": {
    "name": "buvid3",
    "type": "string",
    "default": "",
    "hint": "buvid3 (from Cookie)",
    "locales": {
      "zh": { "name": "buvid3", "hint": "B站Cookie中的buvid3字段" }
    }
  },
  "dedeuserid": {
    "name": "dedeuserid",
    "type": "string",
    "default": "",
    "hint": "dedeuserid (from Cookie)",
    "locales": {
      "zh": { "name": "DedeUserID", "hint": "B站Cookie中的DedeUserID字段" }
    }
  },
  "ac_time_value": {
    "name": "ac_time_value",
    "type": "string",
    "default": "",
    "hint": "ac_time_value (from Cookie or LocalStorage)",
    "locales": {
      "zh": { "name": "ac_time_value", "hint": "B站Cookie或LocalStorage中的ac_time_value字段" }
    }
  }
}
